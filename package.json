{
  "name": "playwright-saucedemo-framework",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "report": "npx playwright show-report",
    "allure:generate": "allure generate ./allure-results --clean -o ./allure-report || true",
    "allure:open": "allure open ./allure-report || true"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "allure-playwright": "^2.0.0"
  },
  "dependencies": {
    "axios": "^1.0.0"
  },
  "description": "Ready-to-run Playwright framework targeted at https://www.saucedemo.com",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
